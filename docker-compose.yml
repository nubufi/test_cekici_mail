version: '3.7'
services:
  scipy_service:
    build:
      context: .
    command: >
      uvicorn main:app --reload --workers 1 --host 0.0.0.0 --port 9001
    ports:
      - 9001:9001
    restart: always
